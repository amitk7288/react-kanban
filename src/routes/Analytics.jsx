import MainContainer from "../components/ui-components/MainContainer";

export default function Analytics() {
  return (
    <MainContainer title={`Analytics`}>
      <div className="flex flex-col gap-4">hello</div>
    </MainContainer>
  );
}
